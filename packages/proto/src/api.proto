syntax = "proto3";

package api;

service ApiProtoService {
  rpc validateApiKey(ValidateApiKeyRequest) returns (ValidateApiKeyResponse);
}

message ValidateApiKeyRequest {
  string hashedApiKey = 1;
}

message ValidateApiKeyResponse {
  bool isValid =  1;
}
